server:
  port: 8080

mybatis-plus:
  mapper-locations: 'classpath:/mapper/**/*Mapper.xml'
  global-config:
    db-config:
      id-type: id_worker
  configuration:
    map-underscore-to-camel-case: true

spring:
  datasource:
    url: jdbc:mysql://shenjy.club:3306/dev?useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    type: org.apache.commons.dbcp.BasicDataSource